#!/usr/bin/env python3
"""Function takes a n argument and returns the floor of the float"""
from math import floor as math_floor


def floor(n: float) -> int:
    """Function takes a n argument and returns the floor of the float"""
    return math_floor(n)
